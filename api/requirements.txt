# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# Database (SQLite only)
# Note: Removed asyncpg and SQLalchemy[asyncio] since we're using SQLite

# Authentication
python-multipart>=0.0.6

# AI Services
openai>=1.3.0
chromadb>=0.4.15
langchain>=0.1.0
langchain-community>=0.0.10
sentence-transformers>=2.2.2
numpy>=1.26.0
huggingface-hub>=0.19.0

# Email (only if actually used)
sendgrid>=6.10.0

# HTTP client  
httpx>=0.25.0

# Testing (development only)
pytest>=7.4.0
pytest-asyncio>=0.21.0 